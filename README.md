# 🧹 Downloads Cleaner

**Downloads Cleaner** — это простой и полезный Python-скрипт, который автоматически наводит порядок в вашей папке **Downloads**.
Файлы сортируются по типам: изображения → в *Images*, документы → в *Documents*, архивы → в *Archives* и т.д.

Это отличный учебный проект, который демонстрирует работу с файловой системой, модулями Python, структурами каталогов и обработкой ошибок.

---

## 🚀 Возможности

* Автоматически сортирует файлы по категориям.
* Категории и расширения настраиваются в `config.py`.
* Создаёт недостающие папки автоматически.
* Защищает от перезаписи файлов.
* Работает на Windows / macOS / Linux.

---

## 📁 Структура проекта

```
downloads_cleaner/
│
├── cleaner/
│   ├── __init__.py
│   ├── config.py            # Конфигурация категорий и расширений
│   ├── organizer.py         # Основная логика сортировки
│   └── main.py              # Точка входа
│
├── README.md
└── requirements.txt (опционально)
```

---

## ⚙️ Настройка пути к папке Downloads

По умолчанию скрипт использует:

```python
downloads_path = Path.home() / "Downloads"
```

Если у вас другой путь — измените его в `main.py`:

```python
downloads_path = Path(r"D:\Users\YourName\Downloads")
```

Или храните путь в `config.py` — как вам удобнее.

---

## ▶️ Как запустить

### 1. Клонируйте проект

```
git clone https://github.com/username/downloads_cleaner.git
cd downloads_cleaner
```

### 2. Убедитесь, что структура пакета корректная

Запуск выполняется **из корня** репозитория.

### 3. Запустите проект

```
python -m cleaner.main
```

И вы увидите примерно такое:

```
📁 Сортировка файлов в: C:\Users\User\Downloads
[OK] photo.jpg → Images/
[OK] presentation.pdf → Documents/
✨ Готово! Папка «Загрузки» приведена в порядок.
```

---

## 🧩 Настройка категорий

Файл `cleaner/config.py`:

```python
CATEGORIES = {
    "Images": [".jpg", ".jpeg", ".png", ".gif", ".svg", ".webp"],
    "Documents": [".pdf", ".docx", ".txt", ".xlsx", ".pptx"],
    "Archives": [".zip", ".rar", ".7z", ".tar", ".gz"],
    "Videos": [".mp4", ".mov", ".avi", ".mkv"],
    "Music": [".mp3", ".wav", ".flac"],
    "Programs": [".exe", ".msi"],
}

DEFAULT_DIR_NAME = "Others"
```

Хотите сортировать PSD, CSV, EPUB? Просто добавьте расширения.

---

## 🧪 Тестирование (опционально)

Вы можете создать набор тестов в директории `tests/` и проверять работу функций с помощью `pytest`.

---


---

## 📜 Лицензия

MIT License — используйте код свободно.

---